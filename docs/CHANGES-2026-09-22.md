# Thunderbolt Ranch — website review changes (2026-09-22)

Tracking list from Max's walkthrough of thunderboltbeef.com.
Status: ☐ open · ☑ done · ⚠ blocked / needs input

## Brand
- ⚠ **Update the Thunderbolt Ranch logo** — waiting on the Dropbox folder of logo
  files. Swap targets: `public/thunderbolt-mark.png` (header mark, 46px tall) and
  `public/thunderbolt-logo.png`. Wired through `ASSET()` in `src/components/Layout.tsx`.

## Front page
- ☑ Link the sample order sheet (`/track/TR-SAMPLE1`) from the front page so people
  can see a finished cut sheet before they start.
- ☑ Each Pricing & Deposits card links to that sample, pre-scoped to the card's share
  (`/track/TR-SAMPLE1?share=quarter|half|whole`).
- ☑ Asterisks on the quarter / half / whole price + weight estimates, with one footnote.
- ☑ Drop the live-weight / hanging-weight / take-home ladder. Lead with the single
  take-home estimate instead.
- ☑ New "versus the grocery store" savings table — final $/lb per cut vs. the shelf
  price, with total savings for a quarter, half, and whole.

## How it works
- ☑ Rewrite "Why this beats the grocery store": less math, lead with total dollars
  saved, then quality, then American / Colorado-local.

## Cut sheet wizard
- ☑ Running ground-beef total at the bottom is bolder and better defined.
- ☑ Cut photo on every question, plus the steer photo with the current primal lit up.
- ☑ **T-bone** — subtitle is just "The middle of the back"; plain-language body copy.
- ☑ **Chuck** — "Most popular" on roast; reworded the January line.
- ☑ **Arm** — "Most popular" on grind.
- ☑ **Sirloin** — "Most popular" on steaks; grind removed; "Sirloin roasts slice thin
  for sandwiches".
- ☑ **Sirloin tip** — "Most popular" on steaks; grind removed; shortened copy.
- ☑ **Top round** — "Most popular" on grind.
- ☑ **Bottom round** — "Most popular" on grind.
- ☑ **Barbecue cuts** — explicit Keep / Grind buttons, no default, both required.
  "Most popular" on Keep except Korean ribs (Grind).
- ☑ **Fast-cooking cuts** — same, "Most popular" on every Keep.
- ☑ **Workhorse cuts** — same, "Most popular" on every Grind.
- ☑ **Ground beef** — 1 lb marked most popular; patty sizes spell out 4 oz = 1/4 lb and
  8 oz = 1/2 lb; pick how many pounds go to patties and see the loose-ground remainder.
- ☑ **Organs & bones** — real check marks on the tiles; new request for the fat (tallow).
- ☑ **Special requests** — moved to its own final step, with a call-out to phone
  Colorado Custom Meat Co after ordering to talk any special request through.

## Also changed along the way
- The Order page's share cards now match the front page: hanging weight dropped, asterisks
  and the footnote added.
- Sirloin and sirloin tip lost their "grind" option entirely, per the notes.
- The savings table hides the constant "your $/lb" column on phones so it fits without
  sideways scrolling.

## Notes for Max
- The CCMC fillable PDF has **no** free-text/notes/fat field. Special requests, tallow
  and patty poundage now print on an appended "SPECIAL REQUESTS" page instead.
- Grocery shelf prices in `GROCERY_CUTS` (`src/data/config.ts`) are survey estimates
  calibrated to the USDA Choice average already on the site. Replace with real observed
  Front Range prices before leaning on the savings number in marketing.
- Where a "most popular" option was called out, it is now also the pre-selected default
  (top round and bottom round now default to grind, sirloin tip to steaks, arm to grind).
  Say the word if you'd rather badge popularity without moving the default.
- **Ground beef is close to break-even against the shelf** at the assumed $8.49/lb for
  85/15 — it's the biggest line by weight, so the whole savings number swings on it. The
  table shows that row as "about even" rather than a fake win. Real shelf prices from a
  Greeley or Fort Collins store would firm this up fast.
- Savings at today's numbers: about $271 on a quarter, $542 on a half, $1,069 on a whole.
